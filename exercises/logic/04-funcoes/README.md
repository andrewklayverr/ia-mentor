# 04 — Decompor uma compra

Implemente `validarItens`, `calcularSubtotal` e `resumirCompra`. Itens têm `{ nome, preco, quantidade }`; resumo: `{ subtotal, frete, total, unidades }`. Não misture impressão com cálculo.

Teste: `npm run check:logic:4`.

