# 05 — Analisar notas

Implemente `analisarNotas(notas)` sem ordenar nem modificar a entrada. Retorne `{ media, maior, menor, aprovadas }`, com aprovação `>= 7`. Rejeite array vazio e notas fora de 0–10.

Primeiro use loop; depois compare com métodos de array. Teste: `npm run check:logic:5`.

