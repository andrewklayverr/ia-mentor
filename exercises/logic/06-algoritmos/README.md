# 06 — Par com soma-alvo

Implemente `encontrarPar(numeros, alvo)`. Retorne índices do primeiro par cuja soma seja o alvo, ou `null`; não reutilize índice.

Faça primeiro `O(n²)`, escreva testes e depois tente `O(n)` com `Map`, explicando o custo de memória. Teste: `npm run check:logic:6`.

