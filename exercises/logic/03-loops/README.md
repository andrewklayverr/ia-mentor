# 03 — Estatísticas de faixa

Implemente `estatisticas(inicio, fim)` para intervalo inclusivo e retorne `{ soma, quantidade, pares }`. Rejeite não inteiros e `inicio > fim`. Escreva antes o invariante do acumulador.

Teste: `npm run check:logic:3`.

